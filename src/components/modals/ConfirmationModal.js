import React from 'react'
import TransitionModal from './TransitionModal'

const ConfirmationModal = ({ title, onConfirm, onReject, ...props }) => {
    return (
        <TransitionModal
            title={title || 'Please Confirm'}
        >
            ehlljjljsfkdjk
        </TransitionModal>
    )
}

export default ConfirmationModal